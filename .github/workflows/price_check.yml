name: Daily Price Check (Cloud)

on:
  schedule:
    # Run at 7:00 AM UTC (midnight PDT)
    - cron: '0 7 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  check-prices:
    runs-on: ubuntu-latest
    
    env:
      NIKE_EMAIL: ${{ secrets.NIKE_EMAIL }}
      NIKE_PASSWORD: ${{ secrets.NIKE_PASSWORD }}
      ALERT_EMAIL: ${{ secrets.ALERT_EMAIL }}
      GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          playwright install chromium
          playwright install-deps

      - name: Run price checker (Nike + Uniqlo only)
        run: python main.py --cloud-only

      - name: Commit updated price history
        run: |
          git config user.name "Price Tracker Bot"
          git config user.email "bot@price-tracker.local"
          git add price_history.json alert_state.json
          git diff --staged --quiet || git commit -m "📊 Cloud price update $(date -u +%Y-%m-%d)"
          git push
